namespace ClrDebug
{
    /// <summary>
    /// Specifies constants used by CoInitializeCor when it initializes the common language runtime.
    /// </summary>
    public enum COINITICOR
    {
        /// <summary>
        /// Indicates the default initialization mode.
        /// </summary>
        COINITCOR_DEFAULT = 0x0           // Default initialization mode. 
    }
}